% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tune_pars_gadget.R
\name{tune_pars}
\alias{tune_pars}
\title{Interactively tune model parameters with a Shiny gadget}
\usage{
tune_pars(pars, age_at_length)
}
\arguments{
\item{pars}{A named list of scalar numeric parameters. Typical entries are
\code{k}, \code{L_inf}, \code{d}, \code{m}, \code{spawning_mu}, \code{spawning_kappa}, \code{annuli_date},
and \code{annuli_min_age}.}

\item{age_at_length}{A data frame of observed age-at-length data as used by
\code{plotAgeLikelihood()} (see \code{Cod_CS_age_at_length}).}
}
\value{
Invisibly returns the final parameters after the gadget is closed.
While the gadget is running it displays the plot interactively.
}
\description{
Launches a small Shiny gadget with sliders in the left sidebar for each
numeric element of \code{pars} and a live-updating plot of
\code{plotAgeLikelihood(pars, age_at_length)} in the main panel.
}
\examples{
# Not run: requires interactive session
# data("Cod_CS_age_at_length")
# data("Cod_CS_pars")
# tune_pars(Cod_CS_pars, Cod_CS_age_at_length)

}
