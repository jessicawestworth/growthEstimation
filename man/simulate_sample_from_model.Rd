% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ageDensity.R
\name{simulate_sample_from_model}
\alias{simulate_sample_from_model}
\title{Simulate a sample from model predictions}
\usage{
simulate_sample_from_model(P_model_K_given_l, survey_obs)
}
\arguments{
\item{P_model_K_given_l}{Matrix of predicted probabilities P(K | length).}

\item{survey_obs}{A data frame for one survey with at least a \code{Length} column.}
}
\value{
An integer vector of simulated K values aligned with \code{survey_obs} rows.
}
\description{
Draws K values using multinomial sampling with probabilities P(K | length)
for each length observed in a given survey.
}
\examples{
set.seed(1)
P <- matrix(c(0.7, 0.3, 0.2, 0.8), nrow = 2, byrow = TRUE)
rownames(P) <- c("20", "30"); colnames(P) <- c("0", "1")
survey_obs <- data.frame(Length = c(20, 20, 30, 30, 30))
simulate_sample_from_model(P, survey_obs)
}
\keyword{internal}
